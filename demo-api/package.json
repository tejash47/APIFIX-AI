{
  "name": "apifix-demo-api",
  "version": "1.0.0",
  "description": "Flawed reference REST API service for APIFIX AI test target",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/auth.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
